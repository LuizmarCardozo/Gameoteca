<Window x:Class="Gameoteca.Views.MessageWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Mensagem" Width="400" 
        MinHeight="180" SizeToContent="Height"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        WindowStyle="None"
        Background="Transparent"
        AllowsTransparency="True">

    <Border Background="#1A0B34" BorderBrush="#4B2A86" BorderThickness="1" CornerRadius="12" Padding="20">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock x:Name="LblTitle" Text="Mensagem" Foreground="White" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,15"/>

            <TextBlock x:Name="LblMessage" Grid.Row="1" Foreground="#D6C8FF" FontSize="14" TextWrapping="Wrap" VerticalAlignment="Top" Margin="0,0,0,25"/>

            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Bottom">
                <Button Content="OK" IsDefault="True" Click="Ok_Click"
                        Background="#A855F7" Foreground="White" BorderThickness="0" 
                        Padding="30,8" Cursor="Hand">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="8"/>
                        </Style>
                    </Button.Resources>
                </Button>
            </StackPanel>
        </Grid>
    </Border>
</Window>