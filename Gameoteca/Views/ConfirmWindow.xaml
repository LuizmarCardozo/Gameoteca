<Window x:Class="Gameoteca.Views.ConfirmWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Confirmar" Width="400" 
        MinHeight="180" SizeToContent="Height"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        WindowStyle="None"
        Background="Transparent"
        AllowsTransparency="True">

    <Border Background="#1A0B34" BorderBrush="#4B2A86" BorderThickness="1" CornerRadius="12" Padding="20">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock x:Name="LblTitle" Text="Confirmar" Foreground="White" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,15"/>

            <TextBlock x:Name="LblMessage" Grid.Row="1" Foreground="#D6C8FF" FontSize="14" TextWrapping="Wrap" VerticalAlignment="Top" Margin="0,0,0,25"/>

            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Bottom">
                <Button Content="Não" IsCancel="True" Click="No_Click"
                        Background="Transparent" Foreground="#D6C8FF" BorderThickness="0" Cursor="Hand" Margin="0,0,10,0"/>

                <Button Content="Sim" IsDefault="True" Click="Yes_Click"
                        Background="#3A1230" Foreground="#FF7AB6" BorderThickness="0" 
                        Padding="20,8" Cursor="Hand">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="8"/>
                        </Style>
                    </Button.Resources>
                </Button>
            </StackPanel>
        </Grid>
    </Border>
</Window>